{
  "schemaVersion": 1,
  "id": "qwen2.5-vl",
  "revision": 1,
  "reviewedAt": "2026-07-19T00:00:00Z",
  "reviewer": "hand-reviewed (Opus) — runnability verified (transformers.js 4.2.0 bundle grep + real load attempt + processor/chat-template/image-token validation in Node), headless Chrome (own harness, --disable-gpu) across the model page + basics/practical/wild/multi-model with desktop+mobile screenshots, honest needs-WebGPU state confirmed",
  "frontend": true,
  "rubric": [
    {
      "dimension": "real-inference",
      "score": 4,
      "severity": "info",
      "evidence": "Runnability proven at the API layer: the Qwen2_5_VLForConditionalGeneration class + qwen2_5_vl model type exist ONLY in @huggingface/transformers 4.2.0 (grep of the 4.2.0 dist bundle matches; the shared 3.7.5 pin has zero matches), so the worker version-pins 4.2.0 LOCALLY (like sam2-segmentation) and imports it directly — never touching lib/webai.js. A real Node load of AutoProcessor.from_pretrained('onnx-community/Qwen2.5-VL-3B-Instruct-ONNX') returned a Qwen2_5_VLProcessor and processed an image into inputs. The WebGPU decoder generate() genuinely needs a GPU adapter; in headless (--disable-gpu) all 5 pages showed the honest loader state 'This model can't run on this device/browser. It needs WebGPU (no GPU adapter here)…' with the Generate control left DISABLED — no fake output.",
      "notes": "Inference runs in models/qwen2.5-vl/worker.js (module Web Worker), dtype { embed_tokens: fp16, vision_encoder: fp16, decoder_model_merged: q4f16 }, device webgpu — mirroring the verified-working built qwen2-vl demo. The live streamed WebGPU generation was not observed on this no-GPU runner; the needs-WebGPU fallback was."
    },
    {
      "dimension": "io-shape",
      "score": 4,
      "severity": "info",
      "evidence": "Input side validated for real in Node (transformers 4.2.0): a 448×336 image kept its native 4:3 aspect (resized to a multiple of 28 → 448×336, NOT squared — dynamic resolution), apply_chat_template produced correct Qwen ChatML with the vision span (<|im_start|>…<|vision_start|><|image_pad|><|vision_end|>…), image_grid_thw came back [1,24,32], and input_ids contained exactly 192 <|image_pad|> tokens (id 151655) = (24×32 patches)/(2×2 merge). The worker's live image-token counter and the see-inside (nw/28)×(nh/28) figure both compute 192 — they agree. Output side (streamed VLM answer coherent with image+prompt) is a GPU-gated manual-evidenced check awaiting a WebGPU device.",
      "notes": "The see-inside surface reports the real image-token count + processed size from these same processor outputs — a genuine, distinct 'dynamic resolution → variable vision-token budget' story vs the fixed-square qwen2-vl demo."
    },
    {
      "dimension": "runtime-config",
      "score": 5,
      "severity": "info",
      "evidence": "At-a-glance shows the exact source id onnx-community/Qwen2.5-VL-3B-Instruct-ONNX (linked to its card + the Qwen/Qwen2.5-VL-3B-Instruct origin), task image-text-to-text, architecture (redesigned ViT: window attention + dynamic resolution + absolute-time mRoPE) + Qwen2.5 decoder, dtype decoder q4f16 / vision+embeddings fp16, backend WebGPU (needs shader-f16), ~2.9 GB, Apache-2.0, and explicitly discloses the Transformers.js 4.2.0 version pin scoped to this demo's worker only. Loader detail line confirms 'onnx-community/Qwen2.5-VL-3B-Instruct-ONNX · transformers.js · ~2900 MB'.",
      "notes": "Honest positioning as DISTINCT from the built qwen2-vl (native aspect vs square, stronger doc/chart OCR, grounding) with a direct cross-link for side-by-side comparison. No invented API — the pipeline snippet matches the real 4.2 class."
    },
    {
      "dimension": "cache-init",
      "score": 4,
      "severity": "info",
      "evidence": "The page loads its model exclusively through the shared createModelLoader auto-init architecture (lib/model-loader.js) — no hand-rolled Load button. On a WebGPU device the capability gate passes and it drives checking → (absent ⇒ Download / cached-current ⇒ auto-init) → ready; the model-cache layer is version-agnostic (scans Cache Storage by modelId) so the 4.2.0 pin does not break auto-init. On this no-GPU runner the honest needs-WebGPU state is reached before the cache state, which is correct (a device that cannot run it should not offer a multi-GB download).",
      "notes": "The current-cached auto-init-no-button pass is inherited from the shared loader (verified on other built demos); it was not separately GPU-exercised here because a GPU is required to reach the ready state."
    },
    {
      "dimension": "states",
      "score": 4,
      "severity": "info",
      "evidence": "role=status aria-live regions announce loader (checking→initialising→ready/unsupported) and per-page generation status ('Generating…', 'Reading…', 'Locating…', 'Done.', and '.err' failures). The honest unsupported/needs-WebGPU state was verified live on all 5 pages with a labelled, readable explanation + enable steps — never a blank panel. Generation try/catch surfaces 'Generation failed: <message>' on the page.",
      "notes": "Download-progress, retry, offline-cached and eviction paths are provided by the shared loader; they need a WebGPU device (to pass the capability gate) to be driven end-to-end, so they were not separately exercised on this runner."
    },
    {
      "dimension": "controls",
      "score": 4,
      "severity": "minor",
      "evidence": "Real, labelled, keyboard-operable controls verified in the DOM/screenshots: sample tray (role=button + Enter/Space handlers, active state), drag/drop dropzone (role=button, keyboard-activatable, file picker), prompt text input, quick-prompt chips (≥2.2rem min tap height), max-tokens number input, Generate/Ask/Read-it/Locate buttons, and the wild page's grounding overlay (role=img, aria-label updated with box count/labels). Every button exposes an accessible name; the run button is correctly disabled until the model is ready.",
      "notes": "A full driven touch traversal at 360×740 (chip taps, dropzone activation) on a GPU device — where controls become enabled — is the remaining check before flipping mobile support to ok."
    },
    {
      "dimension": "no-fake-output",
      "score": 5,
      "severity": "info",
      "evidence": "No canned output anywhere: with no WebGPU the answer areas keep their muted placeholder and the readout stays [hidden] (fixed a CSS specificity bug where .readout{display:flex} overrode [hidden] — now .readout:not([hidden])). The wild page draws boxes ONLY from parseBoxes() over the real streamed coordinate text; if nothing parses it shows the raw output and draws nothing ('No parseable boxes — showing raw output.'), never a fabricated box. The image-token / grid / processed-size figures are the processor's real outputs.",
      "notes": "backend 'WEBGPU' is a static label shown only in the post-run readout, not model output."
    },
    {
      "dimension": "accessibility",
      "score": 5,
      "severity": "info",
      "evidence": "buttons-have-names and images-have-alt hold (sample thumbnails, preview, all carry alt; every button has text). The grounding overlay is an SVG with role=img + a live-updated aria-label; the dropzone is role=button with Enter/Space; loader status is role=status aria-live; sample attributions are shown as visible captions (rights-safe CC-BY/CC-BY-SA attribution). Focus-visible outlines on thumbs/dropzone/chips. Verified in headless DOM checks + screenshots; caret animation respects prefers-reduced-motion.",
      "notes": "Attribution captions double as an accessibility + licensing win: every displayed CC-BY(-SA) media asset names its creator + licence."
    },
    {
      "dimension": "responsive",
      "score": 5,
      "severity": "info",
      "evidence": "Headless matrix at desktop (1280×800) and mobile (360×740, DPR3): every one of the 5 pages had scrollWidth-innerWidth = 0 (no horizontal overflow) on BOTH classes, console-clean (0 errors) and network-clean (0 failed requests). Mobile screenshot (practical) confirms legible reflow of the heading, controls, and the needs-WebGPU panel with no clipping. Layout uses flex-wrap + min-inline-size:0 intrinsic sizing (per css-layout guidance), logical properties, and overflow-wrap:anywhere on answer/glance cells.",
      "notes": "Kept needs-review pending a driven touch/keyboard matrix pass on a GPU device (automated signal only, per the harness rule)."
    },
    {
      "dimension": "performance",
      "score": 4,
      "severity": "info",
      "evidence": "All inference (vision encode + decode + streaming) runs in the module Web Worker (off the main thread) so token streaming never blocks input; the grounding box draw is a handful of SVG rects with vector-effect:non-scaling-stroke (cheap, no per-pixel canvas composite). Headless load stayed console-clean with no long-task errors. Streaming appends tokens incrementally rather than re-rendering the whole answer string as a blocking loop.",
      "notes": "A live INP/long-task trace during a real WebGPU generation would further confirm the token-append path stays under the frame budget on a GPU device."
    }
  ],
  "guidanceConsulted": [
    {
      "id": "css-layout",
      "query": "responsive control panel layout without horizontal overflow",
      "recommendation": "Use flexbox with flex-wrap for item rows, set min-inline-size:0 on flex items holding long/unbreakable content so they can shrink, prefer intrinsic sizing + logical properties, and let long tokens wrap so nothing forces page overflow.",
      "appliedOrException": "applied — .vlm-grid/.controls-row/.chips/.sample-strip are flex-wrap; .vlm-img-col/.vlm-out-col carry min-inline-size:0 and flex:1 1 <basis>; the At-a-glance table cells + answer areas use overflow-wrap:anywhere. Verified desktop+mobile scrollWidth-innerWidth = 0 on all 5 pages.",
      "evidence": "verify-qwen25.mjs: desktopOverflowPx=0 mobileOverflowPx=0 for model/basics/practical/wild/multimodel; /home/paulkinlan/tmp/qwen25-shots/*.png"
    },
    {
      "id": "accessibility",
      "query": "accessible live region status, keyboard-operable custom controls, canvas/overlay image alternatives",
      "recommendation": "Announce async state in a role=status aria-live region; give every control an accessible name; expose non-text visualisations (the SVG box overlay) via role=img + aria-label; make custom controls keyboard-operable and honour prefers-reduced-motion.",
      "appliedOrException": "applied — shared loader status is role=status aria-live; the grounding SVG overlay is role=img with a box-count/label aria-label; sample thumbs + dropzone are role=button with Enter/Space; buttons-have-names + images-have-alt pass; the streaming caret animation is disabled under prefers-reduced-motion.",
      "evidence": "headless DOM checks (buttons-have-names, images-have-alt); role=status + role=img assertions; screenshots"
    },
    {
      "id": "break-up-long-tasks",
      "query": "stream generated tokens without INP regression, keep heavy work off the main thread",
      "recommendation": "Run inference off the main thread and append streamed output incrementally instead of re-rendering large strings in a blocking loop; avoid per-pixel main-thread composites.",
      "appliedOrException": "applied — Qwen2.5-VL vision+decode inference runs in a module Web Worker (worker.js); tokens stream via postMessage and are appended one at a time; the grounding overlay is lightweight SVG (no getImageData/putImageData composite).",
      "evidence": "worker.js new Worker(...,{type:'module'}); inference-off-main-thread source assertion; console-clean headless load"
    },
    {
      "id": "cross-document-transitions",
      "query": "catalogue to model page navigation transitions",
      "recommendation": "Use cross-document view transitions for catalogue↔page navigation.",
      "appliedOrException": "applied — @view-transition navigation:auto in the shared public/styles.css (invariant 6); the demo links to /web-ai-showcase/ and the qwen2-vl comparison page.",
      "evidence": "shared design system (public/styles.css)"
    }
  ],
  "openQuestions": [
    "Drive a real WebGPU generation on a GPU device to record: (a) a coherent streamed answer referencing the image, (b) grounding boxes that parse from real coordinate output and land on the right objects, and (c) an INP/tok-s trace — then flip io-shape/real-inference to 5 and desktop/mobile support to ok.",
    "Qwen2.5-VL grounding coordinate convention: confirmed the worker reports processed dims and parseBoxes handles both JSON bbox_2d and legacy <|box_start|> spans, but the exact scaling (are emitted coords in the pre- or post-smart_resize space?) should be pinned against real GPU output on a few images.",
    "3B q4f16 is ~2.9 GB — confirm mobile-GPU memory headroom (or degrade honestly) on a real WebGPU phone; a 7B variant exists but is likely too large for most mobile GPUs."
  ],
  "followUpGoals": [
    {
      "goal": "GPU verification pass for qwen2.5-vl: real WebGPU run on model+wild pages, capture streamed answer + drawn grounding boxes + tok/s, then flip support to ok and io-shape/real-inference to 5.",
      "kind": "targeted-fix",
      "priority": "high"
    },
    {
      "goal": "Add a 'Qwen2-VL vs Qwen2.5-VL' side-by-side wild demo: run the SAME document through both and show the OCR + native-resolution difference in one view, making the generational upgrade visible rather than described.",
      "kind": "additive-demo",
      "priority": "low"
    },
    {
      "goal": "Add a video-understanding demo (Qwen2.5-VL supports absolute-time video encoding) once a browser-feasible frame-sampling path is proven — sample N frames from a short clip and ask temporal questions.",
      "kind": "additive-demo",
      "priority": "low"
    }
  ],
  "summary": "qwen2.5-vl is an honest, distinct demo of Qwen2.5-VL-3B (onnx-community/Qwen2.5-VL-3B-Instruct-ONNX), materially different from the built qwen2-vl: it keeps images at native aspect ratio (dynamic resolution — validated live: a 4:3 image stays 4:3, 192 vision tokens = image_grid_thw [1,24,32]), reads documents/charts more reliably, and adds visual GROUNDING (parse the model's bbox output → draw SVG boxes, with an honest raw-output fallback). The Qwen2_5_VLForConditionalGeneration class exists only in transformers.js 4.2.0, so the worker version-pins 4.2.0 locally (sam2 precedent) and never touches the shared 3.7.5 pin. Real Node validation confirmed the processor/chat-template/image-token path; headless Chrome across all 5 pages showed the honest needs-WebGPU state (no GPU adapter), console+network clean, zero horizontal overflow desktop+mobile, run control correctly disabled — no fake output. See-inside surfaces the real image-token count, processed size, chat template, and per-token latency. Desktop+mobile responsive-check automated-clean (kept needs-review pending a driven GPU pass). Revision 1."
}
