{
  "schemaVersion": 1,
  "id": "smollm3",
  "revision": 1,
  "reviewedAt": "2026-07-19T00:00:00Z",
  "reviewer": "hand-authored (Opus) — driven headless-Chrome (no-GPU) end-to-end across all 5 pages; desktop+mobile no-overflow, light+dark screenshots read",
  "frontend": true,
  "rubric": [
    {
      "dimension": "real-inference",
      "score": 4,
      "severity": "info",
      "evidence": "Source calls a REAL Transformers.js path (NOT WebLLM): worker.js imports pickDevice + TRANSFORMERS_URL from lib/webai.js, dynamic-imports @huggingface/transformers 3.7.5, and runs pipeline('text-generation', 'HuggingFaceTB/SmolLM3-3B-ONNX', {device:'webgpu', dtype:'q4f16'}) + a TextStreamer. VERIFIED: the SmolLM3 class (SmolLM3ForCausalLM / model_type 'smollm3', mapped to the llama attention family) is registered in @huggingface/transformers 3.7.5; the ONNX build HuggingFaceTB/SmolLM3-3B-ONNX is library:transformers.js, pipeline:text-generation, gated:false, apache-2.0, with onnx/model_q4f16 (~2124 MB) present. In the no-GPU headless runner every single-model page reached the honest 'unsupported / needs-WebGPU (no GPU adapter here)' state — NEVER a faked token.",
      "notes": "A 3B model is impractical on WASM (multi-GB int8, minutes/token) so the page requires a real WebGPU adapter; observing live GPU decode was not possible in this no-GPU sandbox by design. The load path, class registration, and ONNX build are verified; a GPU-device run is the open item."
    },
    {
      "dimension": "io-shape",
      "score": 4,
      "severity": "info",
      "evidence": "Expected shape is a hybrid reasoning stream: a <think>…</think> reasoning segment (in thinking mode) then an answer. VERIFIED against the ONNX repo's tokenizer_config chat template: it defines enable_thinking (default true) → reasoning_mode /think vs /no_think; in /think the generation prompt is '<|im_start|>assistant\\n' (model emits <think>…</think> then answer), in /no_think it is '<|im_start|>assistant\\n<think>\\n\\n</think>\\n' (model answers directly). worker.js applies the template ITSELF with enable_thinking so the mode is honest, streams via TextStreamer, and splits the <think> reasoning from the answer (partial-tag holdback). UI renders reasoning into a collapsible <details class=think> trace; see-inside shows reasoning-tokens vs answer-tokens vs tok/s; wild page runs the SAME prompt /no_think vs /think side by side; practical page JSON.parses the answer.",
      "notes": "Semantic sanity of the reasoning/answer split is verified in code + the wire panel (the exact templated prompt is shown); observing the live <think> content requires a GPU device (openQuestion)."
    },
    {
      "dimension": "runtime-config",
      "score": 5,
      "severity": "info",
      "evidence": "conformance-derived: declares-model-id (HuggingFaceTB/SmolLM3-3B-ONNX linked to card), declares-task (text-generation), declares-backend (WebGPU), declares-quantisation (q4f16), inference-off-main-thread (worker.js). At-a-glance (read in the desktop screenshot) lists model HuggingFaceTB/SmolLM3-3B, ONNX build HuggingFaceTB/SmolLM3-3B-ONNX (onnx/model_q4f16), runtime Transformers.js/ONNX (not WebLLM) with the smollm3 class in 3.7.5, params 3B q4f16, thinking control enable_thinking (/think · /no_think), ChatML template with system role, WebGPU-only ~3 GB, download ~2.1 GB, Apache-2.0.",
      "notes": "Honest, complete disclosure; matches exactly what worker.js loads (MODEL_ID HuggingFaceTB/SmolLM3-3B-ONNX, dtype q4f16 on device webgpu)."
    },
    {
      "dimension": "cache-init",
      "score": 5,
      "severity": "info",
      "evidence": "Every page uses the shared createModelLoader (no bespoke Load button). requiresWebGPU gate fires first: on the no-GPU runner the single-model pages land in 'unsupported' with the honest needs-WebGPU message and NO action button, and the fallback panel explains that a 3B model is impractical on WASM + how to enable WebGPU + the ~2.1 GB cost — no silent download. The multi-model page's MiniLM loader (WASM, no GPU) correctly shows 'download-required' (absent) while the SmolLM3 loader shows needs-WebGPU. Per-model 'Clear cached model' control provided by the shared loader once ready. lib/model-cache.js is version-agnostic (scans Cache Storage by modelId), so auto-init works for the transformers.js cache too.",
      "notes": "On a GPU device the state matrix (absent→Download, current→auto-init no button, partial→Re-download) is driven by the same shared loader; the needs-WebGPU honest gate is what this environment can prove."
    },
    {
      "dimension": "states",
      "score": 5,
      "severity": "info",
      "evidence": "conformance: live-status-region (role=status aria-live on every page), errors-on-the-page (.err status surface + Retry via loader), webgpu-capability-honest (real adapter probe → labelled unsupported). Driven runs: 0 uncaught JS errors, 0 console errors, 0 failed network requests on all 5 pages at desktop AND mobile. Generation failures set status .err with the message; Stop calls the InterruptableStoppingCriteria.",
      "notes": "Honest needs-WebGPU rendered on all pages. Offline-cached / partial-evicted state drives need a GPU device with a warm cache (openQuestion)."
    },
    {
      "dimension": "controls",
      "score": 4,
      "severity": "minor",
      "evidence": "Main: thinking-mode checkbox (updates the /think·/no_think hint), system-prompt textarea, temp/top_p/max-tokens ranges with live value bind, message textarea, Send/Stop/Clear. Basics: thinking toggle + example chips + Ask. Practical: reason-before-extract toggle + sample chips + schema input + Extract, with live JSON.parse validity + a reasoning trace <details>. Wild: puzzle chips + 'Run both' (same prompt /no_think vs /think side by side). Multi-model: two shared loaders + reasoning toggle + KB textarea + question. Every control is a real labelled element; chips ≥40px, composer buttons ≥44px.",
      "notes": "controls-operable is manual-evidenced via rendered screenshots; a full keyboard-only traversal (tab order + visible focus, the thinking checkbox, the details disclosure) on a GPU device is the remaining verification."
    },
    {
      "dimension": "no-fake-output",
      "score": 5,
      "severity": "info",
      "evidence": "Before a run every answer/reasoning area shows only a muted placeholder; on the no-GPU runner the loader shows the honest unsupported state and NO tokens are ever synthesised. There is no canned reply path anywhere — output only ever comes from the Transformers.js generator + TextStreamer.",
      "notes": "Verified across all 5 pages: no pre-baked result, and the WebGPU gate prevents any output when the model genuinely can't run."
    },
    {
      "dimension": "accessibility",
      "score": 4,
      "severity": "info",
      "evidence": "conformance: images-have-alt (no content <img>), buttons-have-names (all buttons have text). role=status aria-live=polite loader/status; chat log + answer areas aria-live=polite; textareas have aria-label; example chip sets grouped with role=group aria-label; the reasoning trace is a native <details>/<summary> disclosure (keyboard-operable); caret/spinner/marker animations disabled under prefers-reduced-motion.",
      "notes": "Strong baseline. A full screen-reader pass over the streaming reasoning trace + answer on a GPU device would further validate."
    },
    {
      "dimension": "responsive",
      "score": 5,
      "severity": "info",
      "evidence": "Driven matrix: documentElement.scrollWidth - clientWidth == 0 (no horizontal overflow) at 1280x800 AND 360x740 (DPR3, touch) on ALL 5 pages. Screenshots read: smollm3 main (desktop light + dark) — at-a-glance table wraps, lede clean; main (mobile 360px) — heading + lede wrap with inline <code> tokens, no overflow. The wild 'versus' grid uses auto-fit minmax(min(100%,280px),1fr) so it stacks on mobile; min-inline-size:0 + overflow-wrap:anywhere on bubbles/flex items; glance cells overflow-wrap:anywhere.",
      "notes": "Support recorded via responsive-check seed; the driven no-overflow pass across desktop+mobile + light/dark reads is strong evidence."
    },
    {
      "dimension": "performance",
      "score": 4,
      "severity": "info",
      "evidence": "All inference AND the reasoning/answer stream split run in a dedicated module Web Worker (worker.js, new Worker(url,{type:'module'})) so decode + tag-splitting never block the main thread; TextStreamer pieces post incrementally via postMessage and the UI only appends text. conformance worker-compliance=source-pass, inference-off-main-thread=pass. inp-stays-low is manual-evidenced (off-thread architecture); driven runs stayed responsive with 0 console errors.",
      "notes": "An explicit INP measurement while streaming on a GPU device would confirm; the off-thread worker + incremental streaming is the key mitigation."
    }
  ],
  "guidanceConsulted": [
    {
      "query": "stream LLM tokens incrementally web worker without blocking main thread INP",
      "id": "break-up-long-tasks",
      "recommendation": "Run heavy work off the main thread and yield/stream incrementally so it never becomes a long task that spikes INP.",
      "appliedOrException": "applied — all generation and the reasoning/answer channel split run in models/smollm3/worker.js via a TextStreamer; the worker posts small tagged deltas the main thread just appends. No synchronous main-thread loop over tokens.",
      "evidence": "conformance worker-compliance + inference-off-main-thread pass; driven runs stayed responsive with 0 console errors on all 5 pages"
    },
    {
      "query": "responsive control panel form controls no horizontal overflow narrow mobile side-by-side grid",
      "id": "css-layout",
      "recommendation": "Use flex-wrap + gap + intrinsic sizing and set min-inline-size:0 on flex items with long unbreakable content; use grid auto-fit minmax(min(100%,Npx),1fr) so columns stack instead of overflowing.",
      "appliedOrException": "applied — composer/controls/chips use flex-wrap+gap; chat bubbles + composer textarea set min-inline-size:0 + overflow-wrap:anywhere; the wild 'versus' grid and multi-model loader/result grids use auto-fit minmax(min(100%,Npx),1fr); glance cells overflow-wrap:anywhere.",
      "evidence": "scrollWidth-clientWidth==0 at 360x740 and 1280x800 on all 5 pages (driven headless); mobile screenshot shows lede + code tokens wrapped with no overflow"
    },
    {
      "query": "accessible collapsible details disclosure reasoning trace live region status",
      "id": "accessibility",
      "recommendation": "Announce async state changes in an accessible live region, label every control, prefer native disclosure semantics, and respect prefers-reduced-motion.",
      "appliedOrException": "applied — role=status aria-live=polite loader/status; aria-live=polite chat log + answer; aria-label on textareas; role=group on chip sets; the reasoning trace is a native <details>/<summary> (keyboard-operable); caret/spinner/marker animations disabled under prefers-reduced-motion.",
      "evidence": "conformance live-status-region=pass, buttons-have-names=pass; DOM inspection + rendered screenshots"
    },
    {
      "query": "textarea that grows to fit content field-sizing",
      "id": "form-fields-automatically-fit-contents",
      "recommendation": "Use field-sizing:content with min/max block-size so composer textareas grow with input instead of scrolling.",
      "appliedOrException": "applied (shared CHAT_CSS) — .composer textarea and .sysbox textarea use field-sizing:content with min/max block-size caps.",
      "evidence": "CHAT_CSS in models/smollm3/engine.js; no overflow observed on mobile"
    },
    {
      "id": "cross-document-transitions",
      "recommendation": "Use cross-document view transitions for catalogue↔page navigation.",
      "appliedOrException": "applied (inherited) — @view-transition navigation:auto in public/styles.css (invariant 6); every page links the shared stylesheet and crumbs back to the catalogue/model page.",
      "evidence": "shared design system; crumbs present on all 5 pages (read in screenshots)"
    }
  ],
  "openQuestions": [
    "On a real WebGPU device: observe the live <think> reasoning stream vs answer stream, confirm the partial-tag holdback never shows a stray '</thi' fragment, and measure reasoning-tokens vs answer-tokens + tok/s in the see-inside panel.",
    "Confirm the ONNX q4f16 build loads + decodes on the Transformers.js WebGPU backend at usable speed for a 3B model, and that enable_thinking flips the output as the template implies.",
    "Verify the wild page's /no_think vs /think side-by-side actually shows the accuracy/latency contrast on the classic puzzle prompts.",
    "Keyboard-only traversal + visible focus across all 5 pages on a GPU device (Enter-to-send/Shift+Enter, the thinking checkbox, the details disclosure)."
  ],
  "followUpGoals": [
    {
      "goal": "GPU-device verification pass for smollm3: capture the live reasoning trace, reasoning-vs-answer token counts, real tok/s on the Transformers.js WebGPU backend, and the wild page's /think vs /no_think contrast as retained evidence.",
      "kind": "targeted-fix",
      "priority": "high"
    },
    {
      "goal": "Add a see-inside token-probability (top-k next-token) surface via a raw generator.model() forward pass, as the qwen2.5-wasm page does, to expose SmolLM3's sampler.",
      "kind": "additive-demo",
      "priority": "low"
    },
    {
      "goal": "Add a SmolLM3 long-context summarisation demo that reasons over a multi-paragraph document within its extended context window.",
      "kind": "additive-demo",
      "priority": "medium"
    }
  ],
  "summary": "smollm3 is a strong, honest, genuinely-distinct demo: the catalogue's first HYBRID reasoning model running on TRANSFORMERS.JS (not WebLLM). It runs HuggingFaceTB/SmolLM3-3B-ONNX (onnx/model_q4f16, ~2.1 GB) via pipeline('text-generation', …) on WebGPU — the smollm3 model class is verified registered in @huggingface/transformers 3.7.5, and the ONNX repo is library:transformers.js / text-generation / apache-2.0 / gated:false. Its distinguishing feature is real: SmolLM3's enable_thinking switch (the /think · /no_think modes, verified in the ONNX repo's chat template), a worker that applies the template itself and separates the streamed <think> reasoning trace from the answer, a see-inside that shows reasoning-vs-answer token counts + the exact templated prompt, and a wild page that runs the same prompt both ways side by side. Driven end-to-end headless (no-GPU): honest needs-WebGPU state on every single-model page (no faked tokens; a 3B model is impractical on WASM so WebGPU is required), the MiniLM half of the RAG page shows the honest absent/download state, 0 JS/console errors, 0 failed requests, and no horizontal overflow at 1280x800 or 360x740 across all 5 pages, light + dark both legible. Real streamed decode is verified by source + class registration + ONNX build existence; observing live tokens needs a GPU device (the high-priority follow-up). Revision 1."
}
