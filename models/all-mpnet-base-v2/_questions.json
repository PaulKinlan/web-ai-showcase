{
  "schemaVersion": 1,
  "id": "all-mpnet-base-v2",
  "revision": 2,
  "reviewedAt": "2026-07-30T15:10:00+01:00",
  "reviewer": "worker (gen-7 text-family-2, correction pass)",
  "frontend": true,
  "rubric": [
    {
      "dimension": "real-inference",
      "score": 5,
      "severity": "info",
      "evidence": "Fresh-Chromium verify-first probe (/tmp/webai-g7-glm-text2-probe.mjs) + schema-2 validator (81/81) dynamic-import @huggingface/transformers@3.7.5, load Xenova/all-mpnet-base-v2 q8 pinned to e086c5e on WASM, and run real batch inference on every route. Worker-bound outputs retained in acceptance-run.json: overview dim=768 norm=3.116, basics cosine=0.572, wild top=queen, multimodel stage1+stage2 both ran."
    },
    {
      "dimension": "io-shape",
      "score": 5,
      "severity": "info",
      "evidence": "feature-extraction pooling:'mean' normalize:false → 768-d; worker L2-normalizes and returns a TRANSFERRED Float64Array. Deterministic PCA with a fixed sign convention + residual check (no random axis flip). Cosine is labelled an uncalibrated, model-specific relatedness score — NOT a universal threshold."
    },
    {
      "dimension": "runtime-config",
      "score": 5,
      "severity": "info",
      "evidence": "At-a-glance declares canonical sentence-transformers/all-mpnet-base-v2 (Apache-2.0) → browser mirror Xenova/all-mpnet-base-v2 @ e086c5e → q8 ONNX CONVERSION (110,086,122 bytes; lossy, not exact), MPNet arch, WASM, canonical 384-token limit. Multi-model reranker pinned to Xenova/ms-marco-MiniLM-L-6-v2 @ a09144355 (23,143,499 bytes, not 90 MB)."
    },
    {
      "dimension": "cache-init",
      "score": 5,
      "severity": "info",
      "evidence": "Every route loads via createModelLoader with a revision-extended cache key; warm-reuse auto-initialises from cache (validator lifecycle-warm-reuse PASS). Release-from-memory + Clear-cached-model controls present on ALL routes and BOTH multi-model stages (dispose/onDispose wired)."
    },
    {
      "dimension": "states",
      "score": 5,
      "severity": "info",
      "evidence": "Schema-2 lifecycle cells PASS: cold download, warm reuse, release-idle→reinit→real inference, clear-cache disposal (Download reappears), failure→Retry→recover (injected via blocked worker fetch, auto-attached), cancellation/supersession (AbortSignal rejects AbortError + post-abort inference ok). Errors surface on the page."
    },
    {
      "dimension": "controls",
      "score": 5,
      "severity": "info",
      "evidence": "Every control real, labelled, keyboard-operable (keyboard-focus PASS all routes). Mutually-conflicting controls disabled while busy on every route. Nearest-label exposes top score + margin and ABSTAINS below disclosed thresholds (score<0.30 or margin<0.04)."
    },
    {
      "dimension": "no-fake-output",
      "score": 5,
      "severity": "info",
      "evidence": "All numeric outputs produced by real worker inference; no canned scores. Wild describes its result as one revision-specific curated-vocabulary observation (NOT a textbook-proof analogy) and each preset swaps in a matching vocabulary. Multi-model reports an honest reorder count (0/4 unchanged is rendered explicitly)."
    },
    {
      "dimension": "accessibility",
      "score": 5,
      "severity": "info",
      "evidence": "Semantic headings/tables, labelled controls, role=status aria-live regions, scope=col/row + title on the matrix, role=img + aria-label on the PCA chart, compact S1…Sn markers with a separate numbered legend (no label collision)."
    },
    {
      "dimension": "responsive",
      "score": 4,
      "severity": "info",
      "evidence": "No horizontal overflow at desktop 1280×800 or mobile 360×740 DPR3 on all 5 routes × light/dark (validator). Practical textarea inline-size:100%; coarse-pointer chips ~40px. 20 full-page screenshots retained (content-visibility forced on during capture so no blank cards). support left needs-review pending PARENT vision inspection (worker has no vision)."
    },
    {
      "dimension": "performance",
      "score": 5,
      "severity": "info",
      "evidence": "All inference AND k-means clustering run off the main thread via worker-protocol.js (bounded single-flight, transfer buffers). break-up-long-tasks guidance satisfied; the dense 768-d math never blocks the UI."
    }
  ],
  "guidanceConsulted": [
    {
      "id": "break-up-long-tasks",
      "query": "off-main-thread text model worker pipeline with model loader progress error retry and responsive control panel without horizontal overflow",
      "recommendation": "Keep heavy computation off the main thread; offload to a Web Worker.",
      "appliedOrException": "Applied — feature-extraction inference AND k-means clustering run in the worker via worker-protocol.js; the page only renders.",
      "evidence": "npx modern-web-guidance@latest retrieve break-up-long-tasks (2026-07-30)."
    },
    {
      "id": "accessibility",
      "query": "accessible live region status progress download and responsive grid control panel without horizontal overflow",
      "recommendation": "Semantic HTML, labelled controls, visible focus, polite live regions; not colour alone.",
      "appliedOrException": "Applied — labelled controls, role=status aria-live, scope headers + title, role=img PCA chart, readable scores alongside colour.",
      "evidence": "npx modern-web-guidance@latest search (match 'accessibility'), 2026-07-30."
    },
    {
      "id": "css-layout",
      "query": "responsive grid control panel without horizontal overflow",
      "recommendation": "Modern intrinsic sizing (auto-fit/minmax grid, flex) for responsive layouts.",
      "appliedOrException": "Applied — auto-fit minmax(300px,1fr) grid; overflow-x:auto matrix; textarea inline-size:100%; no overflow at both viewports.",
      "evidence": "npx modern-web-guidance@latest search 'responsive grid control panel without horizontal overflow' (match 'css-layout'), 2026-07-30."
    }
  ],
  "openQuestions": [
    "Visual polish (spacing, chart legibility, contrast) across route×viewport×theme needs PARENT vision inspection — worker has no vision; all 20 screenshots are manual_parent_required."
  ],
  "followUpGoals": [
    "If a verified default that genuinely reorders is desired for Multi-model, curate a query+passages set where the cross-encoder demonstrably disagrees with cosine."
  ],
  "summary": "Correction pass: migrated to lib/worker-protocol.js (bounded single-flight, cancellation, transfer buffers, deterministic dispose on ALL routes + both stages); made product claims honest (uncalibrated cosine, exploratory clustering with cohesion/stability, nearest-label abstention, Wild = curated-vocab observation, Multi-model reorder count); deterministic PCA with fixed sign; family-specific pinned reranker (23,143,499 bytes); canonical→mirror→q8 lineage; restored the historical conformance rollup (merge-by-slug, 3 suites/69/42/27); schema-2 acceptance (81/81, pinned requests/bytes, lifecycle cells, 20 committed screenshots). Visual support remains needs-review for parent inspection."
}
