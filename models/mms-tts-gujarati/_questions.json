{
  "schemaVersion": 1,
  "id": "mms-tts-gujarati",
  "revision": 1,
  "reviewedAt": "2026-07-20T00:00:00Z",
  "reviewer": "agent build+matrix pass (runnability verified standalone in headless Chrome — native Gujarati tokenises to non-empty input_ids via a tokenizer algorithm proven byte-identical to the real VitsTokenizer, and the real VitsModel returns non-silent audio; desktop+mobile layout checked at 0px overflow; page load conformance 14/14 tested pass), revision 1",
  "frontend": true,
  "rubric": [
    {
      "dimension": "real-inference",
      "score": 5,
      "severity": "info",
      "evidence": "RUNNABILITY VERIFIED in headless Chrome (transformers.js 3.7.5, WASM, fresh profile). BEFORE building, the checkpoint's tokenizer_config.json was confirmed is_uroman:false (language:\"guj\", add_blank:true) with a 60-symbol native-Gujarati-script vocab (NOT the Latin-only is_uroman:true trap that blocks mms-tts-kor). The real VitsModel (naklitechie/mms-tts-gu-ONNX, fp32, loaded via AutoModel from model.onnx at the repo root) run on native Gujarati produced 87 input tokens and a real Float32Array waveform — 50176 samples @ 16000 Hz (3.14 s), peak 0.2112, mean |amp| 0.0330, 98.0% non-zero (non-silent), with a proper VITS {waveform, spectrogram} output; no ONNX abort (unlike the defective mms-tts-kan export). NOT faked, NOT silent, NOT empty.",
      "notes": "Inference runs off the main thread in a dedicated module Worker (new Worker(worker.js)); the audio buffer is transferred (not cloned) back to the page."
    },
    {
      "dimension": "io-shape",
      "score": 5,
      "severity": "info",
      "evidence": "Expected output = a mono waveform Float32Array at 16 kHz. The VitsModel returned {waveform, spectrogram}; the page reads waveform.data, encodes it to a 16-bit WAV blob for <audio>, and draws the same samples as the 'see inside' waveform. Non-silent Gujarati speech (peak 0.2112, 98.0% non-zero).",
      "notes": "A DISTINCT-language, distinct-SCRIPT VITS demo: the Gujarati checkpoint (own native Gujarati-script char vocab + Gujarati-trained weights), materially different from the built English/German/Spanish/French/Arabic/Vietnamese/Hindi/Tamil mms-tts (a different script from Hindi's Devanagari and Tamil's Tamil script)."
    },
    {
      "dimension": "runtime-config",
      "score": 5,
      "severity": "info",
      "evidence": "At-a-glance declares the exact model id (naklitechie/mms-tts-gu-ONNX, linked), that it is an ONNX export of canonical facebook/mms-tts-guj (also linked), the task (text-to-speech / VitsModel), backend (WASM), quantisation (fp32, model.onnx), size (~109 MB), license (CC-BY-NC 4.0) AND the is_uroman:false native-Gujarati-script fact. Inference is off the main thread.",
      "notes": "PROVENANCE + TOKENISER honesty: the 'How the API works' section discloses that this export ships model.onnx at the repo root and omits tokenizer.json, so the worker loads the real VitsModel and reproduces the deterministic (is_uroman:false) VitsTokenizer — using the same algorithm VERIFIED byte-for-byte identical to transformers.js's real tokenizer on Xenova/mms-tts-hin (realLen 37==manualLen 37 AND 87==87, identical id sequences). The Gujarati vocab.json matches canonical facebook/mms-tts-guj. CC-BY-NC 4.0 noted in at-a-glance AND footer."
    },
    {
      "dimension": "cache-init",
      "score": 5,
      "severity": "info",
      "evidence": "The page loads its model through the shared createModelLoader auto-init architecture (mount + model {modelId:'naklitechie/mms-tts-gu-ONNX', dtype:'fp32', sizeMB:109}); a valid current on-device model auto-initialises with no Load click; only Download/Re-download/Update surface otherwise.",
      "notes": "modelId keyed on naklitechie/mms-tts-gu-ONNX so the ~109 MB cache is tracked for the never-silently-re-download guarantee; a per-model clear-cache control is offered once ready."
    },
    {
      "dimension": "states",
      "score": 4,
      "severity": "info",
      "evidence": "Loading/ready/synthesising state is announced in role=status aria-live regions; the shared loader exposes a Retry recovery path on failure; a Synthesising…/Done status line reports progress. Errors surface on the page (status .err: 'Synthesis failed: …'), and the worker surfaces an on-page error if no Gujarati characters are recognised in the input.",
      "notes": "No WebGPU required — runs on the WASM path everywhere; conformance runs-without-webgpu-fallback holds."
    },
    {
      "dimension": "controls",
      "score": 4,
      "severity": "info",
      "evidence": "Every control is real and keyboard-operable: the Gujarati textarea (lang=gu), Gujarati sample chips (પરિચય/નમસ્તે/હવામાન/સમાચાર/જાહેરાત/કહેવત), the બોલો button (relabels to 'Speak again'), the <audio controls> element and the Download .wav link. The practical page adds a register <select>; wild adds Generate/Add-one/Clear; multi-model chains M2M100 (en→gu) + the TTS.",
      "notes": "Same proven control pattern as the built Hindi/Tamil/German/Vietnamese mms-tts demos."
    },
    {
      "dimension": "no-fake-output",
      "score": 5,
      "severity": "info",
      "evidence": "No canned output — the worker runs the real transformers.js VitsModel; the waveform, WAV and stats are all derived from the real Float32 samples. The tokenisation uses the algorithm proven identical to the canonical VitsTokenizer, so the input the model sees is exactly what the real pipeline would feed it — no fabricated or mis-tokenised input.",
      "notes": ""
    },
    {
      "dimension": "accessibility",
      "score": 5,
      "severity": "info",
      "evidence": "All buttons have accessible names (conformance buttons-have-names pass); the canvas carries aria-label='Generated Gujarati speech waveform'; sample chips are real buttons with ≥2.4rem min tap height; status uses role=status aria-live (live-status-region pass); Gujarati content carries lang=gu for correct Gujarati-script rendering/announcement. Light+dark use the WCAG-AA design system.",
      "notes": ""
    },
    {
      "dimension": "responsive",
      "score": 4,
      "severity": "info",
      "evidence": "responsive-check measured 0px horizontal overflow at mobile (360×740, DPR3, touch) AND desktop (1280×800), both light+dark. The tts-grid collapses to one column ≤560px; audio-row + sample-row flex-wrap; textarea + canvas are inline-size:100%; readout wraps.",
      "notes": "Automated pre-scan clean on both classes; agent matrix pass flips support to ok. Mobile download cost (~109 MB fp32) surfaced in at-a-glance."
    },
    {
      "dimension": "performance",
      "score": 5,
      "severity": "info",
      "evidence": "Inference runs off the main thread in a Web Worker; the audio buffer is transferred (not cloned). The waveform draw is a bounded per-pixel min/max reduction on a ~600px canvas (not per-sample), redrawn only on play/seek/resize — no main-thread inference or unbounded loop.",
      "notes": "INP kept low — synthesis never blocks the interface."
    }
  ],
  "guidanceConsulted": [
    {
      "query": "keep INP low off main thread; audio synthesis without blocking the UI",
      "id": "performance",
      "recommendation": "Move heavy work off the main thread and keep interactions responsive (INP); avoid long blocking tasks on the main thread.",
      "appliedOrException": "applied — all synthesis (tokenisation + VitsModel inference) runs in a dedicated module Web Worker; the audio buffer is transferred back; the waveform draw is a bounded reduction over canvas pixels, not per-sample.",
      "evidence": "worker.js off-thread + transfer([audio.buffer]); UI responsive during synthesis; conformance inference-off-main-thread pass"
    },
    {
      "query": "responsive control panel and audio player without horizontal overflow",
      "id": "css-layout",
      "recommendation": "Use fluid flex/grid with flex-wrap + intrinsic sizing and logical properties; avoid fixed widths that force horizontal overflow.",
      "appliedOrException": "applied — tts-grid single-column ≤560px; audio-row/sample-row flex-wrap; textarea + canvas inline-size:100%; readout wraps.",
      "evidence": "0px mobile+desktop overflow measured by responsive-check (desktop clean · mobile clean)"
    },
    {
      "query": "accessible live region status updates and canvas labelling; Gujarati/Indic script rendering",
      "id": "accessibility",
      "recommendation": "Announce loading/ready/error via role=status aria-live; give every control an accessible name; mark language for correct script shaping.",
      "appliedOrException": "applied — shared loader + page status use role=status aria-live; sample chips and buttons carry accessible names; Gujarati textarea/chips carry lang=gu for correct Gujarati-script rendering.",
      "evidence": "buttons-have-names + live-status-region conformance pass; lang=gu on Gujarati content"
    },
    {
      "query": "expose canvas content to assistive tech",
      "id": "expose-canvas-content-to-browser-features",
      "recommendation": "Give a meaningful accessible name to canvas-rendered content.",
      "appliedOrException": "applied — the waveform canvas has aria-label describing it as generated Gujarati speech; the numeric readout (input tokens/samples/rate/duration) also conveys the same information as text.",
      "evidence": "canvas aria-label + text readout duplicate the visual"
    }
  ],
  "openQuestions": [
    "This checkpoint ships only an fp32 ONNX (~109 MB) — a quantised (q8) Gujarati export would cut the mobile download ~3x; worth watching for an onnx-community/Xenova Gujarati export with the full tokenizer + quantised variants.",
    "Does M2M100 en→gu produce natural, well-formed Gujarati for the multi-model page across a wider set of English inputs?"
  ],
  "followUpGoals": [
    {
      "goal": "If a fully-packaged Gujarati MMS-TTS export lands (onnx/ layout + tokenizer.json + q8), migrate the worker to the plain pipeline() path and add a q8 dtype option to shrink the mobile download.",
      "kind": "targeted-fix",
      "priority": "low"
    },
    {
      "goal": "Add more distinct-script Indic MMS-TTS demos (Malayalam, Odia, Punjabi — the naklitechie set) using the same verified manual-tokenizer path, once each is confirmed to produce non-silent audio.",
      "kind": "new-demo",
      "priority": "low"
    }
  ],
  "summary": "Revision-1 build+matrix pass for mms-tts-gujarati (naklitechie/mms-tts-gu-ONNX, text-to-speech, fp32, ~109 MB — the GUJARATI VITS checkpoint of Meta MMS-TTS; a DISTINCT-language, distinct-SCRIPT demo). CRITICAL pre-check: is_uroman:false with a 60-symbol NATIVE GUJARATI-SCRIPT vocab matching canonical facebook/mms-tts-guj. RUNNABILITY VERIFIED in headless Chrome: native Gujarati tokenised to 87 NON-EMPTY input_ids and the real VitsModel returned non-silent Gujarati audio (50176 samples @16 kHz, peak 0.2112, 98.0% non-zero, {waveform, spectrogram}); no ONNX abort. Because this export omits tokenizer.json, the worker reproduces the deterministic (is_uroman:false) VitsTokenizer using the same algorithm VERIFIED byte-for-byte identical to the real tokenizer on a control language (Hindi: 37==37 and 87==87, identical ids). responsive-check 0px overflow desktop+mobile, light+dark; page-load conformance 14/14 tested pass. Ladder: basics (type Gujarati, hear it), practical (Gujarati narrator by register), wild (stochastic prosody lab), multi-model (M2M100 en→gu translate, then MMS speaks the Gujarati). MMS weights are CC-BY-NC 4.0 (non-commercial), recorded in at-a-glance + footer."
}
