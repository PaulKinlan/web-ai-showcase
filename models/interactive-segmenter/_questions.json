{
  "schemaVersion": 1,
  "id": "interactive-segmenter",
  "revision": 2,
  "reviewedAt": "2026-08-01T10:36:00Z",
  "reviewer": "generation-9 correction worker \u2014 raw hash-chained chrome-devtools-mcp partial rerun",
  "frontend": true,
  "rubric": [
    {
      "dimension": "real-inference",
      "score": 3,
      "severity": "major",
      "evidence": "The executable MCP rerun completed exactly 3/10 route/device rows: overview desktop, Practical desktop, and Wild desktop. Basics desktop and Multi-model desktop are blocked; all five mobile rows were not run. Current accepted screenshots remain 6/20 until one fresh-process capture supplies new evidence. The raw event ledger is evidence/mcp-events.ndjson.",
      "notes": "models/interactive-segmenter/worker.js calls InteractiveSegmenter.segment() on a transferred ImageBitmap; no canned output path exists."
    },
    {
      "dimension": "io-shape",
      "score": 4,
      "severity": "minor",
      "evidence": "The three completed desktop MCP rows expose image-resolution foreground confidence plus category mask, selected-pixel count, coverage, point confidence and bounds. The copyable task API example is corrected to confidenceMasks[0], distinct from the raw two-channel model tensor.",
      "notes": "The model card's raw contract is 512\u00d7512\u00d74 input and 512\u00d7512\u00d72 background/foreground logits; MediaPipe maps the task masks back to image resolution."
    },
    {
      "dimension": "runtime-config",
      "score": 4,
      "severity": "minor",
      "evidence": "artifact.json pins tasks-vision 0.10.18 and MagicTouch float32 generation 1683146867404767 (6,227,884 bytes, SHA-256 e24338a\u2026cd25). The hash-chained MCP ledger retains the exact Headless Chrome user agent and network inspections for attempted rows; full 10-row runtime coverage remains incomplete.",
      "notes": "The worker verifies SHA-256 before createFromOptions and reports CPU fallback after a real adapter probe in the accepted runtime."
    },
    {
      "dimension": "cache-init",
      "score": 3,
      "severity": "major",
      "evidence": "The current MCP run proved explicit MagicTouch download and cached readiness on completed desktop rows. Earlier model-authored lifecycle claims are no longer treated as acceptance evidence; offline/release/clear/retry needs a fresh executable MCP lifecycle run.",
      "notes": "The immutable generation is passed as model.revision so no mutable update check can silently replace it."
    },
    {
      "dimension": "states",
      "score": 3,
      "severity": "major",
      "evidence": "Visible ready and inference states are retained for three desktop rows. The second-stage MCP timeout is explicitly blocked. Offline cached inference, offline error, Retry recovery, release and clear-cache remain unverified by the current raw ledger.",
      "notes": "The custom verified fetch emits real byte progress through the shared download component."
    },
    {
      "dimension": "controls",
      "score": 3,
      "severity": "major",
      "evidence": "Raw MCP events prove point click, all four mask mode buttons, both sliders, a rights-safe file upload, a spatial preset, practical export, and keyboard movement/Enter across three desktop routes. Multi-model completion and every mobile control path remain incomplete.",
      "notes": "Canvas has keyboard target movement plus Enter/Space; nine native preset buttons provide a non-pointer alternative."
    },
    {
      "dimension": "no-fake-output",
      "score": 4,
      "severity": "minor",
      "evidence": "The three completed rows show discriminating confidence/coverage from worker inference and the source has no canned result path. Multi-model classification is not accepted in this rerun because its loader wait timed out before a completed state.",
      "notes": "No Multi-model result or screenshot is accepted by the current evidence; fresh executable evidence must prove both MagicTouch and MobileViT stages."
    },
    {
      "dimension": "accessibility",
      "score": 3,
      "severity": "minor",
      "evidence": "Raw MCP accessibility snapshots expose named native controls, canvas description, preset buttons and status output on attempted desktop routes. Screen-reader hardware and mobile accessibility execution were not run.",
      "notes": "No screen-reader hardware session was run; browser accessibility snapshots were used."
    },
    {
      "dimension": "responsive",
      "score": 2,
      "severity": "major",
      "evidence": "The prior wide files labelled mobile were rejected and removed. The bounded rerun retained 6/20 digest-bound desktop light/dark screenshots at 1280px width; 0/10 mobile screenshots are accepted until a fresh MCP process captures real 360\u00d7740 narrow layouts.",
      "notes": "Coarse-pointer targets use 2.75rem minimum block size; controls wrap within a container query."
    },
    {
      "dimension": "performance",
      "score": 3,
      "severity": "minor",
      "evidence": "Inference, confidence reduction and dense RGBA composition remain worker-first in source, with transferred ImageBitmap/RGBA buffers. The current raw MCP ledger does not provide a complete route/device timing matrix and the long MobileViT loader wait hit the MCP protocol timeout.",
      "notes": "Classic rather than module worker is the documented MediaPipe FilesetResolver exception; deterministic terminate closes both tasks."
    }
  ],
  "guidanceConsulted": [
    {
      "source": "modern-web-guidance via npx -y modern-web-guidance@latest (skill version 2026_05_16-c5e78707)",
      "query": "accessible point selection on canvas with keyboard controls and textual coordinates",
      "ids": [
        "accessibility",
        "expose-canvas-content-to-browser-features"
      ],
      "applied": "Used native buttons/inputs, canvas name and description, arrow+Enter keyboard operation, nine point presets, centralized polite status and a semantic tensor table instead of relying on pixels alone. HTML-in-Canvas was not used because it is unsupported; real DOM remains beside the canvas."
    },
    {
      "source": "modern-web-guidance via npx -y modern-web-guidance@latest",
      "query": "responsive image canvas controls mobile touch targets no horizontal overflow",
      "ids": [
        "css"
      ],
      "applied": "Used logical sizing, container queries, wrapping toolbars, 2.75rem coarse-pointer targets, visible focus, touch-action:pan-y, max-inline-size containment and anywhere wrapping for exact hashes/revisions."
    },
    {
      "source": "modern-web-guidance via npx -y modern-web-guidance@latest",
      "query": "large model download progress retry accessible status browser cache",
      "ids": [
        "performance"
      ],
      "applied": "Kept heavy work off-thread, used the shared progress/status component, required explicit first download, verified one Cache Storage copy, and exposed retry/release/clear states."
    }
  ],
  "openQuestions": [
    "Route/device acceptance is incomplete: 3/10 completed, 2/10 blocked, 5/10 not run; screenshots 6/20. The next environment must use a fresh MCP process/config with bounded snapshot polling rather than a long Runtime.callFunctionOn wait.",
    "GPU delegate and physical mobile behavior remain untested; CPU fallback is the only delegate path observed in the completed rows."
  ],
  "followUpGoals": [
    {
      "goal": "Complete the 10-row MCP route/device matrix and 20 digest-bound screenshots in a fresh process using bounded snapshot polling for the MobileViT loader.",
      "kind": "targeted-fix",
      "priority": "high"
    },
    {
      "goal": "Run the same pinned artifact on a physical WebGPU-capable desktop and mobile device to retain GPU-delegate timings.",
      "kind": "targeted-fix",
      "priority": "medium"
    },
    {
      "goal": "Add a negative/background point refinement model as a distinct future demo rather than changing this one-keypoint MagicTouch contract.",
      "kind": "additive-demo",
      "priority": "low"
    }
  ]
}
