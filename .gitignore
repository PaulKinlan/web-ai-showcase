# Ephemeral headless-Chrome profile used by the conformance + responsive harnesses.
.conformance-chrome-profile/

# Conformance manual-evidence screenshots are large + regenerable (run scripts/conformance.mjs).
# The machine results (results.json) + rollup (index.html) ARE committed as the retained record.
reports/conformance/screens/

# Responsive matrix screenshots are heavy (mobile DPR3). Keep the machine record
# (reports/responsive/results.json + index.html) for all 85; retain screenshots only
# for demos with an authored critique that cites them (force-added). Regenerate the
# rest with: node scripts/responsive-check.mjs
reports/responsive/screens/
